<?php




// -----------------------------------------------------------------------------
// Field Formatter

// If a theme registry for field item only, consider name it <Field_Formatter>_item.

// If it's not an inline item or just some text, create a theme registry is usually a better option.

// If theme $items, try $element[0] = array('#theme' => '');
